import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as UpdateEmailFormStories from './update-email.stories'

<Meta of={UpdateEmailFormStories} />

# Email update form

Form dedicated to updating the user's email address.

<Canvas of={UpdateEmailFormStories.Default} />

<Controls />
