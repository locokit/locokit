import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as UpdatePasswordFormStories from './update-password.stories'

<Meta of={UpdatePasswordFormStories} />

# Password update form

Form dedicated to updating the user's password.

<Canvas of={UpdatePasswordFormStories.Default} />

<Controls />
