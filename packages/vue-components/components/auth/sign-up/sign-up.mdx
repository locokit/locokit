import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as SignUpFormStories from './sign-up.stories'

<Meta of={SignUpFormStories} />

# Sign-up form

Simple enrolment form asking the user for a username and an email address only.

<Canvas of={SignUpFormStories.Default} />

<Controls />
