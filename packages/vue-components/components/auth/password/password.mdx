import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as PasswordFormStories from './password.stories'

<Meta of={PasswordFormStories} />

# Password definition form

Form asking the user to define a password and to validate its choice
by entering it a second time.

<Canvas of={PasswordFormStories.Default} />

<Controls />
