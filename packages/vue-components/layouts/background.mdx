import { Canvas, Meta, Controls } from '@storybook/blocks';
 
import * as BackgroundStories from './background.stories';
 
<Meta of={BackgroundStories} />
 
# Background layout

The background layout allows us to define a background image,
a logo image, and links.

A text can also be displayed in the bottom right with the `text-bottom-right` prop,
useful for versioning control.
 
<Canvas of={BackgroundStories.WithImageLogoAndSlot} />

<Controls />