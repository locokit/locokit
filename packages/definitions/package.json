{
  "name": "@locokit/definitions",
  "version": "0.0.2",
  "description": "LocoKit definitions",
  "author": "Mathieu Dartigues",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "vite build && tsc --emitDeclarationOnly",
    "build:watch": "vite build --watch && tsc --emitDeclarationOnly",
    "lint-dist": "eslint dist --fix",
    "vitest:test": "NODE_ENV=test vitest",
    "vitest:ui": "NODE_ENV=test vitest --ui",
    "vitest:coverage": "NODE_ENV=test vitest run --coverage"
  },
  "dependencies": {
    "@feathersjs/typebox": "5.0.10"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.1.3",
    "@vitest/ui": "^1.1.3",
    "dotenv-flow": "^3.2.0",
    "vite-tsconfig-paths": "^4.0.5",
    "vitest": "^1.1.3"
  }
}
