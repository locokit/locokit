import { describe, it } from 'vitest'

describe('migration service', () => {
  it.todo('is well configured')

  it.todo('create a meta model from a distant database')

  it.todo('create a distant database from a meta model')

  it.todo('throw an error if the distant database cannot be created')
  it.todo(
    'throw an error if the distant database cannot be created when changes are made in both direction (meta model evolution + distant db evol°)',
  )
  it.todo(
    'throw an error if the meta model cannot be updated when changes are made in both direction (meta model evolution + distant db evol°)',
  )
})
