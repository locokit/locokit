import { describe, it } from 'vitest'

describe('workflow-run service', () => {
  it.todo('is well configured')

  it.todo('trigger a workflow and store result + output + status')

  it.todo('trigger a workflow that fails and return the error')

  it.todo('commit a workflow transaction only it it is OK')

  it.todo('rollback a workflow transaction if one failure appear')
})
