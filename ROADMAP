**0.0.4**

* ability to store files with a S3 like storage

**0.0.3**

* build a node / browser client

**0.0.2**

* add settings for API
  * title
* make the API available in a docker container
* add CI / CD
* add code coverage
* Swagger documentation
  * ?
* add column type "formula"
  * with a PEG grammar ? or a Monaco like ?
* security / tests
  * forbid the deletion of a table if views are present
